# 安全说明

- 只添加你拥有或获准使用的账号。
- 不要把 `codex-team-data`、账号目录、API Key、`.env`、证书或私钥提交到 GitHub。
- 设置页面只监听 `127.0.0.1`，不要通过端口转发公开到互联网。
- 只在可信项目中运行 Agent。Git worktree 用于避免文件冲突，不是安全沙箱。
- 发现安全问题时，请先通过 GitHub 私人渠道联系仓库维护者，避免在公开 Issue 中粘贴密钥或登录日志。
